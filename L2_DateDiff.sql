-- The DATEDIFF function in SQL is used to calculate the difference between two dates. 
-- It returns the number of units (typically days) between two dates. The result is usually 
-- expressed as a positive or negative integer depending on which date is earlier.

SELECT DATEDIFF('2024-11-30', '2024-11-01');
